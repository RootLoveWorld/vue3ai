// Placeholder WebAssembly module for development
// In production, this would be generated by Emscripten from particles.c

function Module() {
  return Promise.resolve({
    _init_particles: () => console.log('WASM init_particles called (placeholder)'),
    _update_particles: () => console.log('WASM update_particles called (placeholder)'),
    _apply_attraction: () => console.log('WASM apply_attraction called (placeholder)'),
    _get_particles: () => null,
    _get_particle_count: () => 0,
    _cleanup_particles: () => console.log('WASM cleanup_particles called (placeholder)'),
    HEAPF32: { buffer: new ArrayBuffer(0) }
  });
}

Module